============
Installation
============

At the command line::

    $ easy_install django-security-questions

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-security-questions
    $ pip install django-security-questions